initSidebarItems({"fn":[["decompress_public_key","Decompress the compressed public key and return the uncompressed public key. Note: it also removes the 0x04 prefix, so the result is the uncompressed public key without the prefix."]],"struct":[["DKGGovernorWatcher","A DKG Governor watcher for the DKG Substrate runtime. It watches for the DKG Public Key changes and try to update the signature bridge governor."]]});